[Unit]
Description=Spring petclinic application
After=network.target

[Service]

User=spc
ExecStart=/usr/bin/java -jar /tmp/spring-petclinic-2.4.2.jar SuccessExitStatus=143

[Install]
WantedBy=multi-user.target